node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# Orchescope local state written into the audited repository, at any depth
**/.orchescope/state/
**/.orchescope/cache/

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Local packaging output
*.tgz
release/

# Rendered report states, regenerated by pnpm states. Anchored, because the state tables under
# docs/design/states/ are written by hand and are not this directory.
/states/

# Third party corpus source, acquired at its pinned commit and never vendored
corpus/.cache/

# Local git worktrees used for renames and experiments; never part of the product
_rename-worktrees/
